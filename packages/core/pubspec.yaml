name: core
description: Shared models, constants, mock data, and API client for Zomato Clone
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  dio: ^5.4.0
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  flutter_riverpod: ^2.6.1

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
